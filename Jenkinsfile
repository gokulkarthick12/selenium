pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                echo 'Building the first project...'
            }
        }

        stage('Test') {
            steps {
                echo 'Running the tests...'
            }
        }

        stage('Deploy') {
            steps {
                echo 'Deploying the application...'
            }
        }
    }
}
